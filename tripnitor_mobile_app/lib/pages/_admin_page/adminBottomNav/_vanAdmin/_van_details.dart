import 'package:flutter/material.dart';

class VanDetail extends StatelessWidget {
  const VanDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Van Details'),
      ),
    );
  }
}
