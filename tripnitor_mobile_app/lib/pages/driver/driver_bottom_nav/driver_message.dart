import 'package:flutter/material.dart';

class DriverMessagePage extends StatelessWidget {
  const DriverMessagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Driver\'s Message Page'),
      ),
    );
  }
}
